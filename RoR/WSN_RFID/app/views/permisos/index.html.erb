<table class="table table-condensed">
  <thead>
    <tr>
      <th>Titulo</th>
      <th>Acceso Alerta</th>
      <th>Acceso Articulo</th>
      <th>Acceso Atributo</th>
      <th>Acceso Lector</th>
      <th>Acceso Lectura</th>
      <th>Acceso Paquete</th>
      <th>Acceso Permiso</th>
      <th>Acceso Persona</th>
      <th>Acceso Usuario</th>
      <th>Acceso Vehiculo</th>
    </tr>
  </thead>

  <tbody>
    <% @permisos.each do |permiso| %>
      <tr>
         <td><%= permiso.titulo %></td>
        <td><%= check_box('alerta',"0", {:checked => permiso.acceso_alerta}) %></td>
        <td><%= check_box('articulo',"0", {:checked => permiso.acceso_articulo}) %></td>
        <td><%= check_box('atributo',"0", {:checked => permiso.acceso_atributo}) %></td>
        <td><%= check_box('lector',"0", {:checked => permiso.acceso_lector}) %></td>
        <td><%= check_box('lectura',"0", {:checked => permiso.acceso_lectura}) %></td>
        <td><%= check_box('paquete',"0", {:checked => permiso.acceso_paquete}) %></td>
        <td><%= check_box('permiso',"0", {:checked => permiso.acceso_permiso}) %></td>
        <td><%= check_box('persona',"0", {:checked => permiso.acceso_persona}) %></td>
        <td><%= check_box('usuario',"0", {:checked => permiso.acceso_usuario}) %></td>
        <td><%= check_box('vehiculo',"0", {:checked => permiso.acceso_vehiculo}) %></td>
        <td><%= link_to 'Ver', permiso %></td>
        <td><%= link_to 'Editar', edit_permiso_path(permiso) %></td>
        <td><%= link_to 'Destroy', permiso, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Permiso', new_permiso_path %>