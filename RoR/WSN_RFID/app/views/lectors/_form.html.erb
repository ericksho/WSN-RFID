<%= form_for(@lector) do |f| %>
  <% if @lector.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lector.errors.count, "error") %> prohibited this lector from being saved:</h2>

      <ul>
      <% @lector.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
<div class="container">
  <div class="col-md-6 column">
    <form role="form">
		<div class="form-group">
			<%= f.label :nombre %> <%= f.text_field :nombre, :class => "form-control" %>
		</div>
      <div class="form-group">
         <%= f.label 'Posición' %> <%= f.text_field :posicion, :class => "form-control" %>
      </div>
      <%= f.submit @label, :class => "btn btn-success" %>

      <%= link_to 'Volver', lectors_path, :class => "btn btn-default" %>
  </div>
</div>
<% end %>
